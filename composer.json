{
    "name": "voral/joke",
    "description": "A minimal PHP microframework with manual routing and dependency injection container.",
    "type": "library",
    "license": "MIT",
    "version": "1.3.2",
    "autoload": {
        "psr-4": {
            "Vasoft\\Joke\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Vasoft\\Joke\\Tests\\": "tests/"
        }
    },
    "authors": [
        {
            "name": "Alexander Vorobyev",
            "email": "voral.adm@gmail.com"
        }
    ],
    "scripts": {
        "dev": "php -S localhost:8000 -t public/",
        "test": "php vendor/bin/phpunit --display-warnings --display-all-issues",
        "vi:major": "php ./vendor/bin/vs-version-increment major",
        "vi:minor": "php ./vendor/bin/vs-version-increment minor",
        "vi:patch": "php ./vendor/bin/vs-version-increment patch",
        "vi:auto": "php ./vendor/bin/vs-version-increment",
        "vi:list": "php ./vendor/bin/vs-version-increment --list",
        "vi:debug:auto": "php ./vendor/bin/vs-version-increment --debug",
        "stan": "vendor/bin/phpstan analyse src tests",
        "fixer": "vendor/bin/php-cs-fixer fix --dry-run --diff",
        "check": [
            "@fixer",
            "@stan"
        ]
    },
    "scripts-descriptions": {
        "dev": "Start development server on http://localhost:8000",
        "test": "Run PHPUnit tests",
        "vi:major": "Increment major version and update CHANGELOG.md",
        "vi:minor": "Increment minor version and update CHANGELOG.md",
        "vi:patch": "Increment patch version and update CHANGELOG.md",
        "vi:auto": "Auto-detect version increment based on commit analysis",
        "vi:list": "Display registered commit types and scopes",
        "vi:debug:auto": "Preview auto-detected changes without applying them",
        "stan": "Run PHPStan static analysis on the 'src' and 'tests' directories.",
        "fixer": "Check coding standards using PHP-CS-Fixer (dry-run mode).",
        "check": "Run all checks, including coding standards and static analysis."
    },
    "require": {
        "php": "^8.4",
        "ext-dom": "*",
        "ext-libxml": "*"
    },
    "require-dev": {
        "voral/version-increment": "^3.2",
        "phpunit/phpunit": "^13.1.7",
        "php-mock/php-mock-phpunit": "^2.14",
        "friendsofphp/php-cs-fixer": "^3.93",
        "phpstan/phpstan": "^2.1",
        "phpstan/phpstan-phpunit": "^2.0"
    }
}