<?php

declare(strict_types=1);

namespace Vasoft\Joke\Contract\Auth;

/**
 * Интерфейс аутентификатора.
 *
 * Аутентификатор отвечает за определение личности пользователя
 * на основе доступных данных (сессия, токен, cookie и т.д.).
 *
 * Каждый аутентификатор представляет собой отдельный способ
 * аутентификации (например, сессионный, JWT, API-ключ).
 *
 * AuthService последовательно опрашивает все зарегистрированные
 * аутентификаторы до первого успешного результата.
 *
 * @see AuthService
 *
 * @todo Изменить на реальный класс сервиса
 */
interface AuthenticatorInterface
{
    /**
     * Выполняет аутентификацию пользователя.
     *
     * Метод должен проверить наличие учетных данных в запросе/сессии и попытаться идентифицировать пользователя.
     *
     * Если аутентификация не удалась, данные отсутствуют или пользователь
     * не найден, метод ДОЛЖЕН вернуть null. Это позволяет сервису
     * передать управление следующему аутентификатору в цепочке.
     *
     * Исключения должны выбрасываться только при критических ошибках
     * (ошибка БД, неверный формат токена), которые делают дальнейшую
     * проверку невозможной.
     *
     * @return null|UserInterface Объект пользователя при успехе или null
     */
    public function authenticate(): ?UserInterface;
}
